# ========================================
# BLOG APPLICATION ENVIRONMENT VARIABLES
# ========================================

# =================
# BACKEND (API) VARIABLES
# =================

# MongoDB Configuration
# Use MongoDB Atlas connection string for production
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/blog

# Alternative MongoDB Configuration (for local development)
DB_HOST=localhost
DB_PORT=27017
DB_NAME=blog

# JWT Secret Key (REQUIRED for authentication)
# Generate a strong random string for production
JWT_SECRET=your-super-secret-jwt-key-make-it-very-long-and-random-123456789

# Environment Mode
NODE_ENV=development
# NODE_ENV=production  # Use this for production deployment

# Server Port
PORT=8000

# =================
# FRONTEND (CLIENT) VARIABLES
# =================

# API Base URL (React App Environment Variable)
# For development, uncomment the line below:
# REACT_APP_API_URL=http://localhost:8000
# For production single-service deployment, leave this commented out (auto-detected)

# Google Gemini AI API Key (for AI writing assistant)
# Get this from Google AI Studio: https://aistudio.google.com/
REACT_APP_GEMINI_API_KEY=your_gemini_api_key

# =================
# DEVELOPMENT SETUP
# =================
# 1. Copy this file to .env in the root directory
# 2. Copy this file to .env in the api directory
# 3. Copy this file to .env in the client directory
# 4. Update the values with your actual credentials
# 5. Never commit .env files to version control

# =================
# PRODUCTION DEPLOYMENT (RENDER - SINGLE SERVICE)
# =================
# Set these environment variables in your Render dashboard:
# 
# Single Web Service:
# - MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/blog
# - JWT_SECRET=your-super-secret-jwt-key
# - NODE_ENV=production
# - REACT_APP_GEMINI_API_KEY=your_gemini_api_key
# Note: REACT_APP_API_URL is not needed for single-service deployment (auto-detected)

# =================
# SECURITY NOTES
# =================
# - Keep JWT_SECRET secret and never expose it
# - Use strong passwords for MongoDB Atlas
# - Rotate secrets regularly
# - Use environment-specific values
# - Enable MongoDB Atlas IP whitelisting for production 